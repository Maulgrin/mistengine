<?xml version="1.0" encoding="utf-8"?>
<root>
	<windowclass name="threatdetails">
		<placement>
			<size height="40" width="275" />
		</placement>
		<sizelimits>
			<minimum width="200" height="40" />
			<dynamic />
		</sizelimits>
		<script>function onClickDown(button, x, y)
				WindowManager.getTopWindow(self).bringToFront();
			end

			function updateStatPowerDropdown()
				local nHighest = 0;
			
				for i = 1, 6 do
					local sControlName = "statlvl" .. i;
			
					if self[sControlName] and self[sControlName].getValue() == 1 then
						nHighest = i;
					end
				end
			
				if self.stpower_dd then
					if nHighest &gt; 0 then
						self.stpower_dd.setValue(tostring(nHighest));
					else
						self.stpower_dd.setValue("");
					end
				end
			end
		</script>
		<sheetdata>
			<stringfield name="statustag_string">
				<anchored>
					<left offset="49" />
					<top offset="3" />
					<right offset="-76" />
					<size height="25" />
				</anchored>
				<controlline />
				<lineoffset>-1</lineoffset>
				<font>reference-b</font>
			</stringfield>
			<number name="stpower_num">
				<frame name="fielddark" />
				<bounds>283,0,40,30</bounds>
				<invisible />
			</number>
			<buttonfield name="Checkbox1">
				<bounds>1,5,20,20</bounds>
				<state icon="the_add" />
				<state icon="button_checkon" />
				<script>--function getActorNode()
					--	local node = window.getDatabaseNode();
					--
					--	-- If this is inside a list row, climb upward until we find the actor node.
					--	while node do
					--		if DB.getValue(node, "name", "") ~= "" then
					--			return node;
					--		end
					--
					--		node = node.getParent();
					--	end
					--
					--	return nil;
					--end
					--
					--function getActorNameFromNode(nodeActor)
					--	if not nodeActor then
					--		return "Unknown Actor";
					--	end
					--
					--	local rActor = ActorManager.resolveActor(nodeActor);
					--	if rActor then
					--		return ActorManager.getDisplayName(rActor);
					--	end
					--
					--	return DB.getValue(nodeActor, "name", "Unknown Actor");
					--end
					--
					--function getStatusTagName()
					--	if window.statustag_string then
					--		return window.statustag_string.getValue();
					--	end
					--
					--	return "Status Tag";
					--end
					--
					--function sendModMessageToAll(sText)
					--	local msg = {};
					--	msg.font = "msgfont";
					--	msg.text = sText;
					--
					--	Comm.deliverChatMessage(msg);
					--end
					--
					--function onValueChanged()
					--	if getValue() == 1 then
					--		local nPower = 0;
					--
					--		if window.stpower_dd then
					--			nPower = tonumber(window.stpower_dd.getValue()) or 0;
					--		end
					--
					--		if nPower ~= 0 then
					--			local sStatusTag = "Status Tag";
					--
					--			if window.statustag_string then
					--				sStatusTag = window.statustag_string.getValue();
					--			end
					--
					--			ModifierStack.addSlot(sStatusTag, nPower);
					--
					--			local nodeActor = getActorNode();
					--			local sActorName = getActorNameFromNode(nodeActor);
					--
					--			local sMod = tostring(nPower);
					--			if nPower &gt; 0 then
					--				sMod = "+" .. sMod;
					--			end
					--
					--			sendModMessageToAll(sActorName .. " added " .. sMod .. " from " .. sStatusTag);
					--		end
					--	end
					--end
					function getActorNode()
						local node = window.getDatabaseNode();
					
						-- If this is inside a list row, climb upward until we find the actor node.
						while node do
							if DB.getValue(node, "name", "") ~= "" then
								return node;
							end
					
							node = node.getParent();
						end
					
						return nil;
					end
					
					function getActorNameFromNode(nodeActor)
						if not nodeActor then
							return "Unknown Actor";
						end
					
						local rActor = ActorManager.resolveActor(nodeActor);
						if rActor then
							return ActorManager.getDisplayName(rActor);
						end
					
						return DB.getValue(nodeActor, "name", "Unknown Actor");
					end
					
					function getStatusTagName()
						local sStatusTag = "Status Tag";
					
						if window.statustag_string then
							sStatusTag = window.statustag_string.getValue();
						end
					
						if not sStatusTag or sStatusTag == "" then
							sStatusTag = "Status Tag";
						end
					
						return sStatusTag;
					end
					
					function getModifierText(nMod)
						if nMod &gt;= 0 then
							return "+" .. nMod;
						end
					
						return tostring(nMod);
					end
					
					function sendModMessageToAll(sText)
						local msg = {};
						msg.font = "msgfont";
						msg.text = sText;
					
						Comm.deliverChatMessage(msg);
					end
					
					function onClickDown(button, x, y)
						-- onClickDown fires before the checkbox changes.
						-- getValue() == 0 means this click is checking the box.
						-- getValue() == 1 means this click is unchecking the box.
						if getValue() ~= 0 then
							return true;
						end
					
						local nPower = 0;
					
						if window.stpower_dd then
							nPower = tonumber(window.stpower_dd.getValue()) or 0;
						end
					
						if nPower ~= 0 then
							local sStatusTag = getStatusTagName();
					
							ModifierStack.addSlot(sStatusTag, nPower);
					
							local nodeActor = getActorNode();
							local sActorName = getActorNameFromNode(nodeActor);
					
							sendModMessageToAll(sActorName .. " added " .. getModifierText(nPower) .. " from " .. sStatusTag);
						end
					
						return true;
					end
					
					function onValueChanged()
						-- Leave empty.
						-- Do not add ModifierStack or chat message code here.
					end
				</script>
			</buttonfield>
			<buttonfield name="Checkbox2">
				<bounds>25,5,20,20</bounds>
				<state icon="the_minus" />
				<state icon="button_checkon" />
				<script>--function getActorNode()
					--	local node = window.getDatabaseNode();
					--
					--	-- If this is inside a list row, climb upward until we find the actor node.
					--	while node do
					--		if DB.getValue(node, "name", "") ~= "" then
					--			return node;
					--		end
					--
					--		node = node.getParent();
					--	end
					--
					--	return nil;
					--end
					--
					--function getActorNameFromNode(nodeActor)
					--	if not nodeActor then
					--		return "Unknown Actor";
					--	end
					--
					--	local rActor = ActorManager.resolveActor(nodeActor);
					--	if rActor then
					--		return ActorManager.getDisplayName(rActor);
					--	end
					--
					--	return DB.getValue(nodeActor, "name", "Unknown Actor");
					--end
					--
					--function getStatusTagName()
					--	if window.statustag_string then
					--		return window.statustag_string.getValue();
					--	end
					--
					--	return "Status Tag";
					--end
					--
					--function sendModMessageToAll(sText)
					--	local msg = {};
					--	msg.font = "msgfont";
					--	msg.text = sText;
					--
					--	Comm.deliverChatMessage(msg);
					--end
					--
					--function onValueChanged()
					--	if getValue() == 1 then
					--		local nPower = 0;
					--
					--		if window.stpower_dd then
					--			nPower = tonumber(window.stpower_dd.getValue()) * -1 or 0;
					--		end
					--
					--		if nPower ~= 0 then
					--			local sStatusTag = "Status Tag";
					--
					--			if window.statustag_string then
					--				sStatusTag = window.statustag_string.getValue();
					--			end
					--
					--			ModifierStack.addSlot(sStatusTag, nPower);
					--
					--			local nodeActor = getActorNode();
					--			local sActorName = getActorNameFromNode(nodeActor);
					--
					--			local sMod = tostring(nPower);
					--			if nPower &gt; 0 then
					--				sMod = "+" .. sMod;
					--			end
					--
					--			sendModMessageToAll(sActorName .. " added " .. sMod .. " from " .. sStatusTag);
					--		end
					--	end
					--end
					function getActorNode()
						local node = window.getDatabaseNode();
					
						-- If this is inside a list row, climb upward until we find the actor node.
						while node do
							if DB.getValue(node, "name", "") ~= "" then
								return node;
							end
					
							node = node.getParent();
						end
					
						return nil;
					end
					
					function getActorNameFromNode(nodeActor)
						if not nodeActor then
							return "Unknown Actor";
						end
					
						local rActor = ActorManager.resolveActor(nodeActor);
						if rActor then
							return ActorManager.getDisplayName(rActor);
						end
					
						return DB.getValue(nodeActor, "name", "Unknown Actor");
					end
					
					function getStatusTagName()
						local sStatusTag = "Status Tag";
					
						if window.statustag_string then
							sStatusTag = window.statustag_string.getValue();
						end
					
						if not sStatusTag or sStatusTag == "" then
							sStatusTag = "Status Tag";
						end
					
						return sStatusTag;
					end
					
					function getModifierText(nMod)
						if nMod &gt;= 0 then
							return "+" .. nMod;
						end
					
						return tostring(nMod);
					end
					
					function sendModMessageToAll(sText)
						local msg = {};
						msg.font = "msgfont";
						msg.text = sText;
					
						Comm.deliverChatMessage(msg);
					end
					
					function onClickDown(button, x, y)
						-- onClickDown fires before the checkbox changes.
						-- getValue() == 0 means this click is checking the box.
						-- getValue() == 1 means this click is unchecking the box.
						if getValue() ~= 0 then
							return true;
						end
					
						local nPower = 0;
					
						if window.stpower_dd then
							nPower = tonumber(window.stpower_dd.getValue()) or 0;
							nPower = nPower * -1;
						end
					
						if nPower ~= 0 then
							local sStatusTag = getStatusTagName();
					
							ModifierStack.addSlot(sStatusTag, nPower);
					
							local nodeActor = getActorNode();
							local sActorName = getActorNameFromNode(nodeActor);
					
							sendModMessageToAll(sActorName .. " added " .. getModifierText(nPower) .. " from " .. sStatusTag);
						end
					
						return true;
					end
					
					function onValueChanged()
						-- Leave empty.
						-- Do not add ModifierStack or chat message code here.
					end
				</script>
			</buttonfield>
			<combobox name="stpower_dd">
				<frame name="fieldlight" offset="7,5,7,5" />
				<anchored>
					<top offset="5" />
					<right offset="-43" />
					<size width="26" height="20" />
				</anchored>
				<parameters>
					<labelsres>threatdetails_stpower_dd_Label_1|threatdetails_stpower_dd_Label_2|threatdetails_stpower_dd_Label_3|threatdetails_stpower_dd_Label_4|threatdetails_stpower_dd_Label_5|threatdetails_stpower_dd_Label_6</labelsres>
					<values>1|2|3|4|5|6</values>
				</parameters>
			</combobox>
			<buttonfield name="statlvl1">
				<bounds>55,23,20,20</bounds>
				<state icon="button_checkoff" />
				<state icon="button_checkon" />
				<script>function onValueChanged()
						if window.updateStatPowerDropdown then
							window.updateStatPowerDropdown();
						else
							Debug.chat("updateStatPowerDropdown was not found on parent window");
						end
					end
				</script>
			</buttonfield>
			<buttonfield name="statlvl2">
				<bounds>75,23,20,20</bounds>
				<state icon="button_checkoff" />
				<state icon="button_checkon" />
				<script>function onValueChanged()
						if window.updateStatPowerDropdown then
							window.updateStatPowerDropdown();
						else
							Debug.chat("updateStatPowerDropdown was not found on parent window");
						end
					end
				</script>
			</buttonfield>
			<buttonfield name="statlvl3">
				<bounds>95,23,20,20</bounds>
				<state icon="button_checkoff" />
				<state icon="button_checkon" />
				<script>function onValueChanged()
						if window.updateStatPowerDropdown then
							window.updateStatPowerDropdown();
						else
							Debug.chat("updateStatPowerDropdown was not found on parent window");
						end
					end
				</script>
			</buttonfield>
			<buttonfield name="statlvl4">
				<bounds>115,23,20,20</bounds>
				<state icon="button_checkoff" />
				<state icon="button_checkon" />
				<script>function onValueChanged()
						if window.updateStatPowerDropdown then
							window.updateStatPowerDropdown();
						else
							Debug.chat("updateStatPowerDropdown was not found on parent window");
						end
					end
				</script>
			</buttonfield>
			<buttonfield name="statlvl5">
				<bounds>135,23,20,20</bounds>
				<state icon="button_checkoff" />
				<state icon="button_checkon" />
				<script>function onValueChanged()
						if window.updateStatPowerDropdown then
							window.updateStatPowerDropdown();
						else
							Debug.chat("updateStatPowerDropdown was not found on parent window");
						end
					end
				</script>
			</buttonfield>
			<buttonfield name="statlvl6">
				<bounds>155,23,20,20</bounds>
				<state icon="button_checkoff" />
				<state icon="button_checkon" />
				<script>function onValueChanged()
						if window.updateStatPowerDropdown then
							window.updateStatPowerDropdown();
						else
							Debug.chat("updateStatPowerDropdown was not found on parent window");
						end
					end
				</script>
			</buttonfield>
			<buttoncontrol name="delete_btn">
				<anchored>
					<top offset="6" />
					<right offset="-22" />
					<size width="15" height="15" />
				</anchored>
				<gmvisibleonly />
				<icon normal="button_delete" />
				<script>function onButtonPress()
						local node = window.getDatabaseNode();
					
						if not node then
							window.close();
							return;
						end
					
						if Session.IsHost then
							node.delete();
							return;
						end
					
						local msgOOB = {};
						msgOOB.type = "desktoplist_deleteitem";
						msgOOB.sNode = node.getPath();
					
						Comm.deliverOOBMessage(msgOOB, "");
					end
				</script>
			</buttoncontrol>
		</sheetdata>
	</windowclass>
</root>
